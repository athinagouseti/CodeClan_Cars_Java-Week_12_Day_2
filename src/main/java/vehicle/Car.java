package vehicle;

public class Car extends Vehicle{

    public Car(int price, String colour, int wheels, FuelType fuelType, int doors) {
        super(price, colour, wheels, fuelType, doors);
    }
}
